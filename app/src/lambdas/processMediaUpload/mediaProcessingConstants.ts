/**
 * JPEG quality for source images that thumbnails are generated from,
 * such as the HEIC->JPG conversion and video poster JPGs.
 *
 * High quality (92) since these are archival sources,
 * not served directly to browsers.
 */
export const JPEG_ORIGINAL_QUALITY = 92;
