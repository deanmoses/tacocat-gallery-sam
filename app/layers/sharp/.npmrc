# Install Sharp's native binaries for the Lambda target, not for the machine running
# `sam build`. Without this, building on a Mac packages darwin-arm64 and the function
# fails at import with "Could not load the sharp module using the linux-arm64 runtime".
os=linux
cpu=arm64
libc=glibc
